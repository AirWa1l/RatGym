import React from 'react';

interface NavbarProps {
  user: any;
  onLogout: () => void;
}

// Navbar simplificado ya que el menú está en el sidebar
export const Navbar: React.FC<NavbarProps> = () => {
  return null;
};
