version: "3.9"

services:
  flask-app:
    build: .
    container_name: flask-ai-app
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
